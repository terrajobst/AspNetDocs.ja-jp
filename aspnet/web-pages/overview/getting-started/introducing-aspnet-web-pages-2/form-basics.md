---
uid: web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
title: ASP.NET Web ページの概要-HTML フォームの基礎 |Microsoft Docs
author: Rick-Anderson
description: このチュートリアルでは、ASP.NET Web ページ (Razor) を使用するときに、入力フォームを作成する方法とユーザーの入力を処理する方法の基本について説明します。 これで...
ms.author: riande
ms.date: 05/28/2015
ms.assetid: 81ed82bf-b940-44f1-b94a-555d0cb7cc98
msc.legacyurl: /web-pages/overview/getting-started/introducing-aspnet-web-pages-2/form-basics
msc.type: authoredcontent
ms.openlocfilehash: f57661077ec3bb13f3d4ec41b130bda4d2fb9070
ms.sourcegitcommit: e7e91932a6e91a63e2e46417626f39d6b244a3ab
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 03/06/2020
ms.locfileid: "78463540"
---
# <a name="introducing-aspnet-web-pages---html-form-basics"></a><span data-ttu-id="48820-104">ASP.NET Web ページの概要-HTML フォームの基礎</span><span class="sxs-lookup"><span data-stu-id="48820-104">Introducing ASP.NET Web Pages - HTML Form Basics</span></span>

<span data-ttu-id="48820-105">[Tom FitzMacken](https://github.com/tfitzmac)</span><span class="sxs-lookup"><span data-stu-id="48820-105">by [Tom FitzMacken](https://github.com/tfitzmac)</span></span>

> <span data-ttu-id="48820-106">このチュートリアルでは、ASP.NET Web ページ (Razor) を使用するときに、入力フォームを作成する方法とユーザーの入力を処理する方法の基本について説明します。</span><span class="sxs-lookup"><span data-stu-id="48820-106">This tutorial shows you the basics of how to create an input form and how to handle the user's input when you use ASP.NET Web Pages (Razor).</span></span> <span data-ttu-id="48820-107">データベースが完成したので、フォームのスキルを使用して、ユーザーがデータベース内の特定の映画を検索できるようにします。</span><span class="sxs-lookup"><span data-stu-id="48820-107">And now that you've got a database, you'll use your form skills to let users find specific movies in the database.</span></span> <span data-ttu-id="48820-108">ここでは、 [ASP.NET Web ページを使用したデータの表示の概要につい](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data)て説明します。</span><span class="sxs-lookup"><span data-stu-id="48820-108">It assumes you have completed the series through [Introduction to Displaying Data Using ASP.NET Web Pages](/aspnet/web-pages/overview/getting-started/introducing-aspnet-web-pages-2/displaying-data).</span></span>
> 
> <span data-ttu-id="48820-109">ここでは、次の内容について学習します。</span><span class="sxs-lookup"><span data-stu-id="48820-109">What you'll learn:</span></span>
> 
> - <span data-ttu-id="48820-110">標準の HTML 要素を使用してフォームを作成する方法。</span><span class="sxs-lookup"><span data-stu-id="48820-110">How to create a form by using standard HTML elements.</span></span>
> - <span data-ttu-id="48820-111">フォームでユーザーの入力を読み取る方法。</span><span class="sxs-lookup"><span data-stu-id="48820-111">How to read the user's input in a form.</span></span>
> - <span data-ttu-id="48820-112">ユーザーが指定した検索語句を使用してデータを選択的に取得する SQL クエリを作成する方法について説明します。</span><span class="sxs-lookup"><span data-stu-id="48820-112">How to create a SQL query that selectively gets data by using a search term that the user supplies.</span></span>
> - <span data-ttu-id="48820-113">ページ内のフィールドに、ユーザーが入力した内容を "記憶" する方法。</span><span class="sxs-lookup"><span data-stu-id="48820-113">How to have fields in the page "remember" what the user entered.</span></span>
>   
> 
> <span data-ttu-id="48820-114">説明する機能/テクノロジ:</span><span class="sxs-lookup"><span data-stu-id="48820-114">Features/technologies discussed:</span></span>
> 
> - <span data-ttu-id="48820-115">`Request` オブジェクトです。</span><span class="sxs-lookup"><span data-stu-id="48820-115">The `Request` object.</span></span>
> - <span data-ttu-id="48820-116">SQL `Where` 句。</span><span class="sxs-lookup"><span data-stu-id="48820-116">The SQL `Where` clause.</span></span>

## <a name="what-youll-build"></a><span data-ttu-id="48820-117">作成するアプリケーション:</span><span class="sxs-lookup"><span data-stu-id="48820-117">What You'll Build</span></span>

<span data-ttu-id="48820-118">前のチュートリアルでは、データベースを作成し、データを追加した後、`WebGrid` ヘルパーを使用してデータを表示しました。</span><span class="sxs-lookup"><span data-stu-id="48820-118">In the previous tutorial, you created a database, added data to it, and then used the `WebGrid` helper to display the data.</span></span> <span data-ttu-id="48820-119">このチュートリアルでは、特定のジャンルの映画を検索できる検索ボックスを追加します。タイトルには、入力した単語が含まれています。</span><span class="sxs-lookup"><span data-stu-id="48820-119">In this tutorial, you'll add a search box that lets you find movies of a specific genre or whose title contains whatever word you enter.</span></span> <span data-ttu-id="48820-120">(たとえば、ジャンルが "Action" であるか、タイトルに "Harry" または "Adventure." が含まれているすべての映画を見つけることができます)。</span><span class="sxs-lookup"><span data-stu-id="48820-120">(For example, you'll be able to find all movies whose genre is "Action" or whose title contains "Harry" or "Adventure.")</span></span>

<span data-ttu-id="48820-121">このチュートリアルを完了すると、次のようなページが表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-121">When you're done with this tutorial, you'll have a page like this one:</span></span>

![ジャンルとタイトルの検索を含むムービーページ](form-basics/_static/image1.png)

<span data-ttu-id="48820-123">ページのリスティング部分は、最後のチュートリアルのグリッド &mdash; と同じです。</span><span class="sxs-lookup"><span data-stu-id="48820-123">The listing part of the page is the same as in the last tutorial &mdash; a grid.</span></span> <span data-ttu-id="48820-124">違いは、検索したムービーのみがグリッドに表示されることです。</span><span class="sxs-lookup"><span data-stu-id="48820-124">The difference will be that the grid will show only the movies that you searched for.</span></span>

## <a name="about-html-forms"></a><span data-ttu-id="48820-125">HTML フォームについて</span><span class="sxs-lookup"><span data-stu-id="48820-125">About HTML Forms</span></span>

<span data-ttu-id="48820-126">(HTML フォームの作成に関する経験があり、`GET` と `POST`の違いがある場合は、このセクションを省略できます)。</span><span class="sxs-lookup"><span data-stu-id="48820-126">(If you've got experience with creating HTML forms and with the difference between `GET` and `POST`, you can skip this section.)</span></span>

<span data-ttu-id="48820-127">フォームには、テキストボックス、ボタン、オプションボタン、チェックボックス、ドロップダウンリストなど &mdash; ユーザー入力要素があります。</span><span class="sxs-lookup"><span data-stu-id="48820-127">A form has user input elements &mdash; text boxes, buttons, radio buttons, check boxes, drop-down lists, and so on.</span></span> <span data-ttu-id="48820-128">ユーザーは、ボタンをクリックして、これらのコントロールを入力するか、選択を行い、フォームを送信します。</span><span class="sxs-lookup"><span data-stu-id="48820-128">Users fill in these controls or make selections and then submit the form by clicking a button.</span></span>

<span data-ttu-id="48820-129">次の例では、フォームの基本的な HTML 構文を示しています。</span><span class="sxs-lookup"><span data-stu-id="48820-129">The basic HTML syntax of a form is illustrated by this example:</span></span>

[!code-html[Main](form-basics/samples/sample1.html)]

<span data-ttu-id="48820-130">このマークアップをページで実行すると、次の図のような単純なフォームが作成されます。</span><span class="sxs-lookup"><span data-stu-id="48820-130">When this markup runs in a page, it creates a simple form that looks like this illustration:</span></span>

![ブラウザーに表示される基本的な HTML フォーム](form-basics/_static/image2.png)

<span data-ttu-id="48820-132">`<form>` 要素は、送信される HTML 要素を囲みます。</span><span class="sxs-lookup"><span data-stu-id="48820-132">The `<form>` element encloses HTML elements to be submitted.</span></span> <span data-ttu-id="48820-133">(簡単に作成できないのは、要素をページに追加してから、`<form>` 要素内に配置することです。</span><span class="sxs-lookup"><span data-stu-id="48820-133">(An easy mistake to make is to add elements to the page but then forget to put them inside a `<form>` element.</span></span> <span data-ttu-id="48820-134">この場合、何も送信されません)。`method` 属性は、ユーザー入力を送信する方法をブラウザーに指示します。</span><span class="sxs-lookup"><span data-stu-id="48820-134">In that case, nothing is submitted.) The `method` attribute tells the browser how to submit the user input.</span></span> <span data-ttu-id="48820-135">サーバーで更新を実行する場合、またはサーバーからデータをフェッチするだけの場合は `get` するには、これを `post` に設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-135">You set this to `post` if you're performing an update on the server or to `get` if you're just fetching data from the server.</span></span>

<a id="GET,_POST,_and_HTTP_Verb_Safety"></a>

> [!TIP] 
> 
> <span data-ttu-id="48820-136">**GET、POST、および HTTP 動詞の安全性**</span><span class="sxs-lookup"><span data-stu-id="48820-136">**GET, POST, and HTTP Verb Safety**</span></span>
> 
> <span data-ttu-id="48820-137">HTTP は、ブラウザーとサーバーが情報を交換するために使用するプロトコルで、基本的な操作が非常に単純です。</span><span class="sxs-lookup"><span data-stu-id="48820-137">HTTP, the protocol that browsers and servers use to exchange information, is remarkably simple in its basic operations.</span></span> <span data-ttu-id="48820-138">ブラウザーは、いくつかの動詞のみを使用してサーバーに要求を行います。</span><span class="sxs-lookup"><span data-stu-id="48820-138">Browsers use only a few verbs to make requests to servers.</span></span> <span data-ttu-id="48820-139">Web 用のコードを記述するときは、これらの動詞と、ブラウザーとサーバーでそれらを使用する方法を理解することをお勧めします。</span><span class="sxs-lookup"><span data-stu-id="48820-139">When you write code for the web, it's helpful to understand these verbs and how the browser and server use them.</span></span> <span data-ttu-id="48820-140">最も一般的に使用されている動詞は、次のとおりです。</span><span class="sxs-lookup"><span data-stu-id="48820-140">Far and away the most commonly used verbs are these:</span></span>
> 
> - <span data-ttu-id="48820-141">[https://login.microsoftonline.com/consumers/](`GET`)</span><span class="sxs-lookup"><span data-stu-id="48820-141">`GET`.</span></span> <span data-ttu-id="48820-142">ブラウザーは、この動詞を使用してサーバーから何かをフェッチします。</span><span class="sxs-lookup"><span data-stu-id="48820-142">The browser uses this verb to fetch something from the server.</span></span> <span data-ttu-id="48820-143">たとえば、ブラウザーに URL を入力すると、ブラウザーは `GET` 操作を実行して、必要なページを要求します。</span><span class="sxs-lookup"><span data-stu-id="48820-143">For example, when you type a URL into your browser, the browser performs a `GET` operation to request the page you want.</span></span> <span data-ttu-id="48820-144">ページにグラフィックスが含まれている場合、ブラウザーは画像を取得するために追加の `GET` 操作を実行します。</span><span class="sxs-lookup"><span data-stu-id="48820-144">If the page includes graphics, the browser performs additional `GET` operations to get the images.</span></span> <span data-ttu-id="48820-145">`GET` 操作でサーバーに情報を渡す必要がある場合、情報はクエリ文字列の URL の一部として渡されます。</span><span class="sxs-lookup"><span data-stu-id="48820-145">If the `GET` operation has to pass information to the server, the information is passed as part of the URL in the query string.</span></span>
> - <span data-ttu-id="48820-146">[https://login.microsoftonline.com/consumers/](`POST`)</span><span class="sxs-lookup"><span data-stu-id="48820-146">`POST`.</span></span> <span data-ttu-id="48820-147">ブラウザーは、サーバー上で追加または変更されるデータを送信するために `POST` 要求を送信します。</span><span class="sxs-lookup"><span data-stu-id="48820-147">The browser sends a `POST` request in order to submit data to be added or changed on the server.</span></span> <span data-ttu-id="48820-148">たとえば、`POST` 動詞は、データベース内のレコードを作成したり、既存のレコードを変更したりするために使用されます。</span><span class="sxs-lookup"><span data-stu-id="48820-148">For example, the `POST` verb is used to create records in a database or change existing ones.</span></span> <span data-ttu-id="48820-149">ほとんどの場合、フォームに入力して [送信] ボタンをクリックすると、ブラウザーによって `POST` 操作が実行されます。</span><span class="sxs-lookup"><span data-stu-id="48820-149">Most of the time, when you fill in a form and click the submit button, the browser performs a `POST` operation.</span></span> <span data-ttu-id="48820-150">`POST` 操作では、サーバーに渡されるデータはページの本文に含まれます。</span><span class="sxs-lookup"><span data-stu-id="48820-150">In a `POST` operation, the data being passed to the server is in the body of the page.</span></span>
> 
> <span data-ttu-id="48820-151">これらの動詞の重要な違いは、`GET` 操作ではサーバー上の何も変更されないことです。つまり、`GET` 操作によってサーバー上の状態が変更されることはありません。</span><span class="sxs-lookup"><span data-stu-id="48820-151">An important distinction between these verbs is that a `GET` operation is not supposed to change anything on the server — or to put it in a slightly more abstract way, a `GET` operation does not result in a change in state on the server.</span></span> <span data-ttu-id="48820-152">同じリソースに対して `GET` 操作を何度でも実行でき、それらのリソースは変更されません。</span><span class="sxs-lookup"><span data-stu-id="48820-152">You can perform a `GET` operation on the same resources as many times as you like, and those resources don't change.</span></span> <span data-ttu-id="48820-153">(`GET` 操作は、"安全" と呼ばれることがよくあります。また、技術的な用語を使用する場合は、*べき等*です)。これとは対照的に、`POST` 要求では、操作を実行するたびにサーバー上の何かが変更されます。</span><span class="sxs-lookup"><span data-stu-id="48820-153">(A `GET` operation is often said to be "safe," or to use a technical term, is *idempotent*.) In contrast, of course, a `POST` request changes something on the server each time you perform the operation.</span></span>
> 
> <span data-ttu-id="48820-154">この区別を示す2つの例があります。</span><span class="sxs-lookup"><span data-stu-id="48820-154">Two examples will help illustrate this distinction.</span></span> <span data-ttu-id="48820-155">Bing や Google などのエンジンを使用して検索を実行する場合は、1つのテキストボックスで構成されるフォームに入力し、[検索] ボタンをクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-155">When you perform a search using an engine like Bing or Google, you fill in a form that consists of one text box, and then you click the search button.</span></span> <span data-ttu-id="48820-156">ブラウザーは、URL の一部として渡されたボックスに入力した値を使用して、`GET` 操作を実行します。</span><span class="sxs-lookup"><span data-stu-id="48820-156">The browser performs a `GET` operation, with the value you entered into the box passed as part of the URL.</span></span> <span data-ttu-id="48820-157">この種類のフォームに対して `GET` 操作を使用することは問題ありません。検索操作によってサーバー上のリソースが変更されることはないため、情報をフェッチするだけです。</span><span class="sxs-lookup"><span data-stu-id="48820-157">Using a `GET` operation for this type of form is fine, because a search operation doesn't change any resources on the server, it just fetches information.</span></span>
> 
> <span data-ttu-id="48820-158">次に、何かをオンラインで注文するプロセスを検討します。</span><span class="sxs-lookup"><span data-stu-id="48820-158">Now consider the process of ordering something online.</span></span> <span data-ttu-id="48820-159">注文の詳細を入力し、[送信] ボタンをクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-159">You fill in the order details and then click the submit button.</span></span> <span data-ttu-id="48820-160">この操作は `POST` 要求になります。これは、新しい注文レコード、アカウント情報の変更、およびその他の多くの変更などがサーバーで変更されるためです。</span><span class="sxs-lookup"><span data-stu-id="48820-160">This operation will be a `POST` request, because the operation will result in changes on the server, such as a new order record, a change in your account information, and perhaps many other changes.</span></span> <span data-ttu-id="48820-161">`GET` 操作とは異なり、要求を再送信するたびに、サーバーに新しい注文が生成された場合は、`POST` 要求を繰り返すことはできません。</span><span class="sxs-lookup"><span data-stu-id="48820-161">Unlike the `GET` operation, you cannot repeat your `POST` request — if you did, each time you resubmitted the request, you'd generate a new order on the server.</span></span> <span data-ttu-id="48820-162">(このような場合、web サイトでは、[送信] ボタンを複数回クリックしないことを警告することがよくあります。または、誤ってフォームを再送信しないように、[送信] ボタンが無効になります)。</span><span class="sxs-lookup"><span data-stu-id="48820-162">(In cases like this, websites will often warn you not to click a submit button more than once, or will disable the submit button so that you don't resubmit the form accidentally.)</span></span>
> 
> <span data-ttu-id="48820-163">このチュートリアルでは、HTML フォームを操作するために、`GET` 操作と `POST` 操作の両方を使用します。</span><span class="sxs-lookup"><span data-stu-id="48820-163">In the course of this tutorial, you'll use both a `GET` operation and a `POST` operation to work with HTML forms.</span></span> <span data-ttu-id="48820-164">ここでは、使用する動詞が適切なものである場合について説明します。</span><span class="sxs-lookup"><span data-stu-id="48820-164">We'll explain in each case why the verb you use is the appropriate one.</span></span>
> 
> <span data-ttu-id="48820-165">(HTTP 動詞の詳細については、W3C サイトの[メソッド定義](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html)に関する記事を参照してください)。</span><span class="sxs-lookup"><span data-stu-id="48820-165">(To learn more about HTTP verbs, see the [Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site.)</span></span>

<span data-ttu-id="48820-166">ほとんどのユーザー入力要素は HTML `<input>` 要素です。</span><span class="sxs-lookup"><span data-stu-id="48820-166">Most user input elements are HTML `<input>` elements.</span></span> <span data-ttu-id="48820-167">`<input type="type" name="name">,` のようになります。ここで、 *type*はユーザー入力コントロールの種類を示します。</span><span class="sxs-lookup"><span data-stu-id="48820-167">They look like `<input type="type" name="name">,` where *type* indicates the kind of user input control you want.</span></span> <span data-ttu-id="48820-168">一般的な要素は次のとおりです。</span><span class="sxs-lookup"><span data-stu-id="48820-168">These elements are the common ones:</span></span>

- <span data-ttu-id="48820-169">テキストボックス: `<input type="text">`</span><span class="sxs-lookup"><span data-stu-id="48820-169">Text box: `<input type="text">`</span></span>
- <span data-ttu-id="48820-170">チェックボックス: `<input type="check">`</span><span class="sxs-lookup"><span data-stu-id="48820-170">Check box: `<input type="check">`</span></span>
- <span data-ttu-id="48820-171">オプションボタン: `<input type="radio">`</span><span class="sxs-lookup"><span data-stu-id="48820-171">Radio button: `<input type="radio">`</span></span>
- <span data-ttu-id="48820-172">ボタン: `<input type="button">`</span><span class="sxs-lookup"><span data-stu-id="48820-172">Button: `<input type="button">`</span></span>
- <span data-ttu-id="48820-173">送信ボタン: `<input type="submit">`</span><span class="sxs-lookup"><span data-stu-id="48820-173">Submit button: `<input type="submit">`</span></span>

<span data-ttu-id="48820-174">`<textarea>` 要素を使用して複数行のテキストボックスを作成し、`<select>` 要素を使用してドロップダウンリストまたはスクロール可能な一覧を作成することもできます。</span><span class="sxs-lookup"><span data-stu-id="48820-174">You can also use the `<textarea>` element to create a multiline text box and the `<select>` element to create a drop-down list or scrollable list.</span></span> <span data-ttu-id="48820-175">(HTML フォーム要素の詳細については、「W3Schools サイトでの[Html フォームと入力](http://www.w3schools.com/html/html_forms.asp)」を参照してください)。</span><span class="sxs-lookup"><span data-stu-id="48820-175">(For more about HTML form elements, see [HTML Forms and Input](http://www.w3schools.com/html/html_forms.asp) on the W3Schools site.)</span></span>

<span data-ttu-id="48820-176">`name` 属性は、後で説明するように、後で要素の値を取得する方法であるため、非常に重要です。</span><span class="sxs-lookup"><span data-stu-id="48820-176">The `name` attribute is very important, because the name is how you'll get the value of the element later, as you'll see shortly.</span></span>

<span data-ttu-id="48820-177">興味深いのは、ユーザーの入力を使用した、ページの開発者です。</span><span class="sxs-lookup"><span data-stu-id="48820-177">The interesting part is what you, the page developer, do with the user's input.</span></span> <span data-ttu-id="48820-178">これらの要素に関連付けられた組み込みの動作はありません。</span><span class="sxs-lookup"><span data-stu-id="48820-178">There's no built-in behavior associated with these elements.</span></span> <span data-ttu-id="48820-179">代わりに、ユーザーが入力または選択した値を取得し、その値を使用して操作を行う必要があります。</span><span class="sxs-lookup"><span data-stu-id="48820-179">Instead, you have to get the values that the user has entered or selected and do something with them.</span></span> <span data-ttu-id="48820-180">このチュートリアルで学習する内容は次のようになります。</span><span class="sxs-lookup"><span data-stu-id="48820-180">That's what you'll learn in this tutorial.</span></span>

> [!TIP] 
> 
> <span data-ttu-id="48820-181">**HTML5 と入力フォーム**</span><span class="sxs-lookup"><span data-stu-id="48820-181">**HTML5 and Input Forms**</span></span>
> 
> <span data-ttu-id="48820-182">ご存知のように、HTML は移行中であり、最新バージョン (HTML5) には、ユーザーが情報を入力するためのより直感的な方法がサポートされています。</span><span class="sxs-lookup"><span data-stu-id="48820-182">As you might know, HTML is in transition and the latest version (HTML5) includes support for more intuitive ways for users to enter information.</span></span> <span data-ttu-id="48820-183">たとえば、HTML5 では、ユーザーが日付を入力するように求めるページを表示することができます。</span><span class="sxs-lookup"><span data-stu-id="48820-183">For example, in HTML5, you (the page developer) can tell the page that you want the user to enter a date.</span></span> <span data-ttu-id="48820-184">ブラウザーは、ユーザーが手動で日付を入力することを要求するのではなく、自動的に予定表を表示できます。</span><span class="sxs-lookup"><span data-stu-id="48820-184">The browser can then automatically display a calendar rather than requiring the user to enter a date manually.</span></span> <span data-ttu-id="48820-185">ただし、HTML5 は新しいものであり、すべてのブラウザーでまだサポートされていません。</span><span class="sxs-lookup"><span data-stu-id="48820-185">However, HTML5 is new and is not supported in all browsers yet.</span></span>
> 
> <span data-ttu-id="48820-186">ASP.NET Web ページは、ユーザーのブラウザーが行っているエクステントへの HTML5 入力をサポートしています。</span><span class="sxs-lookup"><span data-stu-id="48820-186">ASP.NET Web Pages supports HTML5 input to the extent that the user's browser does.</span></span> <span data-ttu-id="48820-187">HTML5 の `<input>` 要素の新しい属性の概念については、W3Schools サイトの「 [HTML &lt;入力&gt; Type 属性](http://www.w3schools.com/html/html_form_input_types.asp)」を参照してください。</span><span class="sxs-lookup"><span data-stu-id="48820-187">For an idea of the new attributes for the `<input>` element in HTML5, see [HTML &lt;input&gt; type Attribute](http://www.w3schools.com/html/html_form_input_types.asp) on the W3Schools site.</span></span>

## <a name="creating-the-form"></a><span data-ttu-id="48820-188">フォームの作成</span><span class="sxs-lookup"><span data-stu-id="48820-188">Creating the Form</span></span>

<span data-ttu-id="48820-189">WebMatrix の **[ファイル]** ワークスペースで、[*ムービー* ] ページを開きます。</span><span class="sxs-lookup"><span data-stu-id="48820-189">In WebMatrix, in the **Files** workspace, open the *Movies.cshtml* page.</span></span>

<span data-ttu-id="48820-190">終了 `</h1>` タグの後、`grid.GetHtml` 呼び出しの開始 `<div>` タグの前に、次のマークアップを追加します。</span><span class="sxs-lookup"><span data-stu-id="48820-190">After the closing `</h1>` tag and before the opening `<div>` tag of the `grid.GetHtml` call, add the following markup:</span></span>

[!code-html[Main](form-basics/samples/sample2.html)]

<span data-ttu-id="48820-191">このマークアップにより、`searchGenre` という名前のテキストボックスと [送信] ボタンを持つフォームが作成されます。</span><span class="sxs-lookup"><span data-stu-id="48820-191">This markup creates a form that has a text box named `searchGenre` and a submit button.</span></span> <span data-ttu-id="48820-192">テキストボックスと送信ボタンは、`method` 属性が `get`に設定されている `<form>` 要素で囲まれています。</span><span class="sxs-lookup"><span data-stu-id="48820-192">The text box and submit button are enclosed in a `<form>` element whose `method` attribute is set to `get`.</span></span> <span data-ttu-id="48820-193">(テキストボックスと [送信] ボタンを `<form>` 要素内に配置しないと、ボタンをクリックしても何も送信されないことに注意してください)。ここでは `GET` 動詞を使用します。これは、サーバーに変更を加えないフォームを作成しているためです。これにより、検索結果が表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-193">(Remember that if you don't put the text box and submit button inside a `<form>` element, nothing will be submitted when you click the button.) You use the `GET` verb here because you're creating a form that does not make any changes on the server — it just results in a search.</span></span> <span data-ttu-id="48820-194">(前のチュートリアルでは、`post` 方法を使用しています。これは、サーバーに変更を送信する方法です。</span><span class="sxs-lookup"><span data-stu-id="48820-194">(In the previous tutorial, you used a `post` method, which is how you submit changes to the server.</span></span> <span data-ttu-id="48820-195">これについては、次のチュートリアルでもう一度説明します)。</span><span class="sxs-lookup"><span data-stu-id="48820-195">You'll see that in the next tutorial again.)</span></span>

<span data-ttu-id="48820-196">ページを実行します。</span><span class="sxs-lookup"><span data-stu-id="48820-196">Run the page.</span></span> <span data-ttu-id="48820-197">フォームの動作を定義していなくても、次のように表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-197">Although you haven't defined any behavior for the form, you can see what it looks like:</span></span>

![ジャンルの検索ボックスがあるムービーページ](form-basics/_static/image3.png)

<span data-ttu-id="48820-199">テキストボックスに値を入力します ("コメディ" など)。</span><span class="sxs-lookup"><span data-stu-id="48820-199">Enter a value into the text box, like "Comedy."</span></span> <span data-ttu-id="48820-200">**[ジャンルの検索]** をクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-200">Then click **Search Genre**.</span></span>

<span data-ttu-id="48820-201">ページの URL をメモしておきます。</span><span class="sxs-lookup"><span data-stu-id="48820-201">Take note of the URL of the page.</span></span> <span data-ttu-id="48820-202">`<form>` 要素の `method` 属性を `get`に設定するため、入力した値は URL のクエリ文字列の一部になります。次にその例を示します。</span><span class="sxs-lookup"><span data-stu-id="48820-202">Because you set the `<form>` element's `method` attribute to `get`, the value you entered is now part of the query string in the URL, like this:</span></span>

`http://localhost:45661/Movies.cshtml?searchGenre=Comedy`

## <a name="reading-form-values"></a><span data-ttu-id="48820-203">フォーム値の読み取り</span><span class="sxs-lookup"><span data-stu-id="48820-203">Reading Form Values</span></span>

<span data-ttu-id="48820-204">このページには、データベースデータを取得し、結果をグリッドに表示するコードが既に含まれています。</span><span class="sxs-lookup"><span data-stu-id="48820-204">The page already contains some code that gets database data and displays the results in a grid.</span></span> <span data-ttu-id="48820-205">次に、検索語句を含む SQL クエリを実行できるように、テキストボックスの値を読み取るコードを追加する必要があります。</span><span class="sxs-lookup"><span data-stu-id="48820-205">Now you have to add some code that reads the value of the text box so you can run a SQL query that includes the search term.</span></span>

<span data-ttu-id="48820-206">フォームのメソッドを `get`に設定するため、次のようなコードを使用して、テキストボックスに入力された値を読み取ることができます。</span><span class="sxs-lookup"><span data-stu-id="48820-206">Because you set the form's method to `get`, you can read the value that was entered into the text box by using code like the following:</span></span>

`var searchTerm = Request.QueryString["searchGenre"];`

<span data-ttu-id="48820-207">`Request.QueryString` オブジェクト (`Request` オブジェクトの `QueryString` プロパティ) には、`GET` 操作の一部として送信された要素の値が含まれます。</span><span class="sxs-lookup"><span data-stu-id="48820-207">The `Request.QueryString` object (the `QueryString` property of the `Request` object) includes the values of elements that were submitted as part of the `GET` operation.</span></span> <span data-ttu-id="48820-208">`Request.QueryString` プロパティには、フォームで送信される値の*コレクション*(リスト) が格納されます。</span><span class="sxs-lookup"><span data-stu-id="48820-208">The `Request.QueryString` property contains a *collection* (a list) of the values that are submitted in the form.</span></span> <span data-ttu-id="48820-209">個々の値を取得するには、必要な要素の名前を指定します。</span><span class="sxs-lookup"><span data-stu-id="48820-209">To get any individual value, you specify the name of the element that you want.</span></span> <span data-ttu-id="48820-210">このため、テキストボックスを作成する `<input>` 要素 (`searchTerm`) に `name` 属性を設定する必要があります。</span><span class="sxs-lookup"><span data-stu-id="48820-210">That's why you have to have a `name` attribute on the `<input>` element (`searchTerm`) that creates the text box.</span></span> <span data-ttu-id="48820-211">(`Request` オブジェクトの詳細については、後の「[サイドバー](#BKMK_TheRequestObject) 」を参照してください)。</span><span class="sxs-lookup"><span data-stu-id="48820-211">(For more about the `Request` object, see the [sidebar](#BKMK_TheRequestObject) later.)</span></span>

<span data-ttu-id="48820-212">テキストボックスの値を読み取るだけで十分です。</span><span class="sxs-lookup"><span data-stu-id="48820-212">It's simple enough to read the value of the text box.</span></span> <span data-ttu-id="48820-213">ただし、ユーザーがテキストボックスに何も入力せずに **[検索]** をクリックした場合は、検索するものがないため、このクリックを無視できます。</span><span class="sxs-lookup"><span data-stu-id="48820-213">But if the user didn't enter anything at all in the text box but clicked **Search** anyway, you can ignore that click, since there's nothing to search.</span></span>

<span data-ttu-id="48820-214">これらの条件を実装する方法を示すコード例を次に示します。</span><span class="sxs-lookup"><span data-stu-id="48820-214">The following code is an example that shows how to implement these conditions.</span></span> <span data-ttu-id="48820-215">(このコードを追加する必要はありません。しばらくしてから作成してください)。</span><span class="sxs-lookup"><span data-stu-id="48820-215">(You don't have to add this code yet; you'll do that in a moment.)</span></span>

[!code-csharp[Main](form-basics/samples/sample3.cs)]

<span data-ttu-id="48820-216">テストは次のように分割されます。</span><span class="sxs-lookup"><span data-stu-id="48820-216">The test breaks down in this way:</span></span>

- <span data-ttu-id="48820-217">`Request.QueryString["searchGenre"]`の値、つまり `searchGenre`という名前の `<input>` 要素に入力した値を取得します。</span><span class="sxs-lookup"><span data-stu-id="48820-217">Get the value of `Request.QueryString["searchGenre"]`, namely the value that was entered into the `<input>` element named `searchGenre`.</span></span>
- <span data-ttu-id="48820-218">`IsEmpty` メソッドを使用して空であるかどうかを確認します。</span><span class="sxs-lookup"><span data-stu-id="48820-218">Find out if it's empty by using the `IsEmpty` method.</span></span> <span data-ttu-id="48820-219">このメソッドは、何らかの値 (たとえば、form 要素) に値が含まれているかどうかを判断するための標準的な方法です。</span><span class="sxs-lookup"><span data-stu-id="48820-219">This method is the standard way to determine whether something (for example, a form element) contains a value.</span></span> <span data-ttu-id="48820-220">しかし、実際には、空で*ない*場合にのみ注意してください。</span><span class="sxs-lookup"><span data-stu-id="48820-220">But really, you care only if it's *not* empty, therefore ...</span></span>
- <span data-ttu-id="48820-221">`IsEmpty` テストの前に `!` 演算子を追加します。</span><span class="sxs-lookup"><span data-stu-id="48820-221">Add the `!` operator in front of the `IsEmpty` test.</span></span> <span data-ttu-id="48820-222">(`!` 演算子は、論理 NOT) を意味します。</span><span class="sxs-lookup"><span data-stu-id="48820-222">(The `!` operator means logical NOT).</span></span>

<span data-ttu-id="48820-223">英語では、`if` 条件全体が次のように変換されます。*フォームの searchGenre 要素が空でない場合、...*</span><span class="sxs-lookup"><span data-stu-id="48820-223">In plain English, the entire `if` condition translates into the following: *If the form's searchGenre element is not empty, then ...*</span></span>

<span data-ttu-id="48820-224">このブロックは、検索語句を使用するクエリを作成するためのステージを設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-224">This block sets the stage for creating a query that uses the search term.</span></span> <span data-ttu-id="48820-225">これは、次のセクションで行います。</span><span class="sxs-lookup"><span data-stu-id="48820-225">You'll do that in the next section.</span></span>

<a id="BKMK_TheRequestObject"></a>

> [!TIP] 
> 
> <span data-ttu-id="48820-226">**要求オブジェクト**</span><span class="sxs-lookup"><span data-stu-id="48820-226">**The Request Object**</span></span>
> 
> <span data-ttu-id="48820-227">`Request` オブジェクトには、ページが要求または送信されたときにブラウザーがアプリケーションに送信するすべての情報が含まれています。</span><span class="sxs-lookup"><span data-stu-id="48820-227">The `Request` object contains all the information that the browser sends to your application when a page is requested or submitted.</span></span> <span data-ttu-id="48820-228">このオブジェクトには、テキストボックスの値やアップロードするファイルなど、ユーザーが指定した情報がすべて含まれます。</span><span class="sxs-lookup"><span data-stu-id="48820-228">This object includes any information that the user provides, like text box values or a file to upload.</span></span> <span data-ttu-id="48820-229">また、cookie、URL クエリ文字列の値 (存在する場合)、実行中のページのファイルパス、ユーザーが使用しているブラウザーの種類、ブラウザーで設定されている言語の一覧など、あらゆる種類の追加情報が含まれています。などです。</span><span class="sxs-lookup"><span data-stu-id="48820-229">It also includes all sorts of additional information, like cookies, values in the URL query string (if any), the file path of the page that is running, the type of browser that the user is using, the list of languages that are set in the browser, and much more.</span></span>
> 
> <span data-ttu-id="48820-230">`Request` オブジェクトは、値の*コレクション*(リスト) です。</span><span class="sxs-lookup"><span data-stu-id="48820-230">The `Request` object is a *collection* (list) of values.</span></span> <span data-ttu-id="48820-231">コレクションから個別の値を取得するには、その名前を指定します。</span><span class="sxs-lookup"><span data-stu-id="48820-231">You get an individual value out of the collection by specifying its name:</span></span>
> 
> `var someValue = Request["name"];`
> 
> <span data-ttu-id="48820-232">`Request` オブジェクトは、実際にいくつかのサブセットを公開します。</span><span class="sxs-lookup"><span data-stu-id="48820-232">The `Request` object actually exposes several subsets.</span></span> <span data-ttu-id="48820-233">次に例を示します。</span><span class="sxs-lookup"><span data-stu-id="48820-233">For example:</span></span>
> 
> - <span data-ttu-id="48820-234">要求が `POST` 要求の場合、`Request.Form` は、送信された `<form>` 要素内の要素からの値を提供します。</span><span class="sxs-lookup"><span data-stu-id="48820-234">`Request.Form` gives you values from elements inside the submitted `<form>` element if the request is a `POST` request.</span></span>
> - <span data-ttu-id="48820-235">`Request.QueryString` には、URL のクエリ文字列の値だけが表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-235">`Request.QueryString` gives you just the values in the URL's query string.</span></span> <span data-ttu-id="48820-236">(`http://mysite/myapp/page?searchGenre=action&page=2`のような URL では、URL の `?searchGenre=action&page=2` セクションはクエリ文字列です)。</span><span class="sxs-lookup"><span data-stu-id="48820-236">(In a URL like `http://mysite/myapp/page?searchGenre=action&page=2`, the `?searchGenre=action&page=2` section of the URL is the query string.)</span></span>
> - <span data-ttu-id="48820-237">`Request.Cookies` コレクションを使用すると、ブラウザーによって送信された cookie にアクセスできます。</span><span class="sxs-lookup"><span data-stu-id="48820-237">`Request.Cookies` collection gives you access to cookies that the browser has sent.</span></span>
> 
> <span data-ttu-id="48820-238">送信されたフォームにあることがわかっている値を取得するには、`Request["name"]`を使用します。</span><span class="sxs-lookup"><span data-stu-id="48820-238">To get a value that you know is in the submitted form, you can use `Request["name"]`.</span></span> <span data-ttu-id="48820-239">または、より具体的なバージョン `Request.Form["name"]` (`POST` 要求の場合) または `Request.QueryString["name"]` (`GET` 要求の場合) を使用することもできます。</span><span class="sxs-lookup"><span data-stu-id="48820-239">Alternatively, you can use the more specific versions `Request.Form["name"]` (for `POST` requests) or `Request.QueryString["name"]` (for `GET` requests).</span></span> <span data-ttu-id="48820-240">もちろん、 *name*は取得する項目の名前です。</span><span class="sxs-lookup"><span data-stu-id="48820-240">Of course, *name* is the name of the item to get.</span></span>
> 
> <span data-ttu-id="48820-241">取得する項目の名前は、使用しているコレクション内で一意である必要があります。</span><span class="sxs-lookup"><span data-stu-id="48820-241">The name of the item you want to get has to be unique within the collection you're using.</span></span> <span data-ttu-id="48820-242">そのため、`Request` オブジェクトは `Request.Form` や `Request.QueryString`などのサブセットを提供します。</span><span class="sxs-lookup"><span data-stu-id="48820-242">That's why the `Request` object provides the subsets like `Request.Form` and `Request.QueryString`.</span></span> <span data-ttu-id="48820-243">ページに `userName` という名前のフォーム要素が含まれており、`userName`という名前の cookie*も*含まれているとします。</span><span class="sxs-lookup"><span data-stu-id="48820-243">Suppose that your page contains a form element named `userName` and *also* contains a cookie named `userName`.</span></span> <span data-ttu-id="48820-244">`Request["userName"]`を取得した場合、フォーム値と cookie のどちらを使用するかによってあいまいになります。</span><span class="sxs-lookup"><span data-stu-id="48820-244">If you get `Request["userName"]`, it's ambiguous whether you want the form value or the cookie.</span></span> <span data-ttu-id="48820-245">ただし、`Request.Form["userName"]` または `Request.Cookie["userName"]`の場合は、取得する値を明示的に指定しています。</span><span class="sxs-lookup"><span data-stu-id="48820-245">However, if you get `Request.Form["userName"]` or `Request.Cookie["userName"]`, you're being explicit about which value to get.</span></span>
> 
> <span data-ttu-id="48820-246">`Request.Form` や `Request.QueryString`など、興味のある `Request` のサブセットを使用することをお勧めします。</span><span class="sxs-lookup"><span data-stu-id="48820-246">It's a good practice to be specific and use the subset of `Request` that you're interested in, like `Request.Form` or `Request.QueryString`.</span></span> <span data-ttu-id="48820-247">このチュートリアルで作成している単純なページでは、実際には何の違いもありません。</span><span class="sxs-lookup"><span data-stu-id="48820-247">For the simple pages that you're creating in this tutorial, it probably doesn't really make any difference.</span></span> <span data-ttu-id="48820-248">ただし、より複雑なページを作成する場合は、明示的なバージョン `Request.Form` または `Request.QueryString` を使用すると、ページにフォーム (または複数のフォーム)、cookie、クエリ文字列値などが含まれている場合に発生する問題を回避するのに役立ちます。</span><span class="sxs-lookup"><span data-stu-id="48820-248">However, as you create more complex pages, using the explicit version `Request.Form` or `Request.QueryString` can help you avoid problems that can arise when the page contains a form (or multiple forms), cookies, query string values, and so on.</span></span>

## <a name="creating-a-query-by-using-a-search-term"></a><span data-ttu-id="48820-249">検索用語を使用してクエリを作成する</span><span class="sxs-lookup"><span data-stu-id="48820-249">Creating a Query by Using a Search Term</span></span>

<span data-ttu-id="48820-250">これで、ユーザーが入力した検索用語を取得する方法がわかったので、それを使用するクエリを作成できます。</span><span class="sxs-lookup"><span data-stu-id="48820-250">Now that you know how to get the search term that the user entered, you can create a query that uses it.</span></span> <span data-ttu-id="48820-251">すべてのムービー項目をデータベースから取得するには、次のステートメントのような SQL クエリを使用していることに注意してください。</span><span class="sxs-lookup"><span data-stu-id="48820-251">Remember that to get all the movie items out of the database, you're using a SQL query that looks like this statement:</span></span>

`SELECT * FROM Movies`

<span data-ttu-id="48820-252">特定の映画だけを取得するには、`Where` 句を含むクエリを使用する必要があります。</span><span class="sxs-lookup"><span data-stu-id="48820-252">To get only certain movies, you have to use a query that includes a `Where` clause.</span></span> <span data-ttu-id="48820-253">この句を使用すると、クエリによって返される行の条件を設定できます。</span><span class="sxs-lookup"><span data-stu-id="48820-253">This clause lets you set a condition on which rows are returned by the query.</span></span> <span data-ttu-id="48820-254">次に例を示します。</span><span class="sxs-lookup"><span data-stu-id="48820-254">Here's an example:</span></span>

`SELECT * FROM Movies WHERE Genre = 'Action'`

<span data-ttu-id="48820-255">基本形式は `WHERE column = value`です。</span><span class="sxs-lookup"><span data-stu-id="48820-255">The basic format is `WHERE column = value`.</span></span> <span data-ttu-id="48820-256">検索対象に応じて、`>` (より大きい)、`<` (より小さい)、`<>` (等しくない)、`<=` (以下) などの `=`だけでなく、さまざまな演算子を使用することもできます。</span><span class="sxs-lookup"><span data-stu-id="48820-256">You can use different operators besides just `=`, like `>` (greater than), `<` (less than), `<>` (not equal to), `<=` (less than or equal to), etc., depending on what you're looking for.</span></span>

<span data-ttu-id="48820-257">注意が必要な場合は、SQL ステートメントの大文字と小文字は区別されません &mdash; `SELECT` は `Select` (または `select`) と同じです。</span><span class="sxs-lookup"><span data-stu-id="48820-257">In case you're wondering, SQL statements are not case sensitive &mdash; `SELECT` is the same as `Select` (or even `select`).</span></span> <span data-ttu-id="48820-258">ただし、多くの場合、`SELECT` や `WHERE`のように、SQL ステートメントのキーワードを大文字にすると、読みやすくなります。</span><span class="sxs-lookup"><span data-stu-id="48820-258">However, people often capitalize keywords in a SQL statement, like `SELECT` and `WHERE`, to make it easier to read.</span></span>

### <a name="passing-the-search-term-as-a-parameter"></a><span data-ttu-id="48820-259">検索用語をパラメーターとして渡す</span><span class="sxs-lookup"><span data-stu-id="48820-259">Passing the search term as a parameter</span></span>

<span data-ttu-id="48820-260">特定のジャンルを検索するのは簡単ですが (`WHERE Genre = 'Action'`)、ユーザーが入力したジャンルを検索できるようにする必要があります。</span><span class="sxs-lookup"><span data-stu-id="48820-260">Searching for a specific genre is easy enough (`WHERE Genre = 'Action'`), but you want to be able to search for any genre that the user enters.</span></span> <span data-ttu-id="48820-261">これを行うには、検索する値のプレースホルダーを含む SQL クエリとしてを作成します。</span><span class="sxs-lookup"><span data-stu-id="48820-261">To do that, you create as SQL query that includes a placeholder for the value to search.</span></span> <span data-ttu-id="48820-262">このコマンドは次のようになります。</span><span class="sxs-lookup"><span data-stu-id="48820-262">It will look like this command:</span></span>

`SELECT * FROM Movies WHERE Genre = @0`

<span data-ttu-id="48820-263">プレースホルダーは `@` 文字の後に0が続きます。</span><span class="sxs-lookup"><span data-stu-id="48820-263">The placeholder is the `@` character followed by zero.</span></span> <span data-ttu-id="48820-264">推測されるように、クエリには複数のプレースホルダーを含めることができ、`@0`、`@1`、`@2`などの名前が付けられます。</span><span class="sxs-lookup"><span data-stu-id="48820-264">As you might guess, a query can contain multiple placeholders, and they'd be named `@0`, `@1`, `@2`, etc.</span></span>

<span data-ttu-id="48820-265">クエリを設定し、実際に値を渡すには、次のようなコードを使用します。</span><span class="sxs-lookup"><span data-stu-id="48820-265">To set up the query and actually pass it the value, you use the code like the following:</span></span>

[!code-sql[Main](form-basics/samples/sample4.sql)]

<span data-ttu-id="48820-266">このコードは、グリッドにデータを表示するために既に実行したものと似ています。</span><span class="sxs-lookup"><span data-stu-id="48820-266">This code is similar to what you've already done to display data in the grid.</span></span> <span data-ttu-id="48820-267">唯一の違いは次のとおりです。</span><span class="sxs-lookup"><span data-stu-id="48820-267">The only differences are:</span></span>

- <span data-ttu-id="48820-268">このクエリにはプレースホルダー (`WHERE Genre = @0"`) が含まれています。</span><span class="sxs-lookup"><span data-stu-id="48820-268">The query contains a placeholder (`WHERE Genre = @0"`).</span></span>
- <span data-ttu-id="48820-269">クエリは変数 (`selectCommand`) に格納されます。前に、クエリを `db.Query` メソッドに直接渡しました。</span><span class="sxs-lookup"><span data-stu-id="48820-269">The query is put into a variable (`selectCommand`); before, you passed the query directly to the `db.Query` method.</span></span>
- <span data-ttu-id="48820-270">`db.Query` メソッドを呼び出す場合は、プレースホルダーに使用するクエリと値の両方を渡します。</span><span class="sxs-lookup"><span data-stu-id="48820-270">When you call the `db.Query` method, you pass both the query and the value to use for the placeholder.</span></span> <span data-ttu-id="48820-271">(クエリに複数のプレースホルダーがある場合は、それらすべてを個別の値としてメソッドに渡します)。</span><span class="sxs-lookup"><span data-stu-id="48820-271">(If the query had multiple placeholders, you'd pass them all as separate values to the method.)</span></span>

<span data-ttu-id="48820-272">これらの要素をすべて一緒に配置すると、次のコードが表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-272">If you put all these elements together, you get the following code:</span></span>

[!code-csharp[Main](form-basics/samples/sample5.cs)]

> [!NOTE] 
> 
> <span data-ttu-id="48820-273">**重要!**</span><span class="sxs-lookup"><span data-stu-id="48820-273">**Important!**</span></span> <span data-ttu-id="48820-274">プレースホルダー (`@0`など) を使用して SQL コマンドに値を渡すことは、セキュリティのために*非常に重要*です。</span><span class="sxs-lookup"><span data-stu-id="48820-274">Using placeholders (like `@0`) to pass values to a SQL command is *extremely important* for security.</span></span> <span data-ttu-id="48820-275">ここに表示される方法は、変数データのプレースホルダーを使用して、SQL コマンドを構築する唯一の方法です。</span><span class="sxs-lookup"><span data-stu-id="48820-275">The way you see it here, with placeholders for variable data, is the only way you should construct SQL commands.</span></span>
> 
> <span data-ttu-id="48820-276">リテラルテキストとユーザーから取得した値を連結 (連結) することで、SQL ステートメントを作成しないようにします。</span><span class="sxs-lookup"><span data-stu-id="48820-276">Never construct a SQL statement by putting together (concatenating) literal text and values you get from the user.</span></span> <span data-ttu-id="48820-277">ユーザー入力を SQL ステートメントに連結すると、サイトが*sql インジェクション攻撃*に対して開かれます。この攻撃では、悪意のあるユーザーが、データベースをハッキングするためのページに値を送信します。</span><span class="sxs-lookup"><span data-stu-id="48820-277">Concatenating user input into a SQL statement opens your site to a *SQL injection attack* where a malicious user submits values to your page that hack your database.</span></span> <span data-ttu-id="48820-278">(詳細については、MSDN web サイトの「 [SQL インジェクション](https://msdn.microsoft.com/library/ms161953.aspx)」を参照してください)。</span><span class="sxs-lookup"><span data-stu-id="48820-278">(You can read more in the article [SQL Injection](https://msdn.microsoft.com/library/ms161953.aspx) the MSDN website.)</span></span>

## <a name="updating-the-movies-page-with-search-code"></a><span data-ttu-id="48820-279">検索コードを使用したムービーページの更新</span><span class="sxs-lookup"><span data-stu-id="48820-279">Updating the Movies Page with Search Code</span></span>

<span data-ttu-id="48820-280">これで、*ムービーの cshtml*ファイルのコードを更新できるようになりました。</span><span class="sxs-lookup"><span data-stu-id="48820-280">Now you can update the code in the *Movies.cshtml* file.</span></span> <span data-ttu-id="48820-281">まず、ページの先頭にあるコードブロック内のコードを次のコードに置き換えます。</span><span class="sxs-lookup"><span data-stu-id="48820-281">To begin, replace the code in the code block at the top of the page with this code:</span></span>

[!code-csharp[Main](form-basics/samples/sample6.cs)]

<span data-ttu-id="48820-282">ここでの違いは、クエリを `selectCommand` 変数に配置したことです。これは後 `db.Query` に渡すことができます。</span><span class="sxs-lookup"><span data-stu-id="48820-282">The difference here is that you've put the query into the `selectCommand` variable, which you'll pass to `db.Query` later.</span></span> <span data-ttu-id="48820-283">SQL ステートメントを変数に指定すると、ステートメントを変更できます。このステートメントは、検索を実行するために使用します。</span><span class="sxs-lookup"><span data-stu-id="48820-283">Putting the SQL statement into a variable lets you change the statement, which is what you'll do to perform the search.</span></span>

<span data-ttu-id="48820-284">また、これらの2つの行を削除しました。これらは後で後で使用します。</span><span class="sxs-lookup"><span data-stu-id="48820-284">You've also removed these two lines, which you'll put back in later:</span></span>

[!code-csharp[Main](form-basics/samples/sample7.cs)]

<span data-ttu-id="48820-285">クエリをまだ実行しない (つまり、`db.Query`を呼び出す) のではなく、`WebGrid` ヘルパーをまだ初期化する必要はありません。</span><span class="sxs-lookup"><span data-stu-id="48820-285">You don't want to run the query yet (that is, call `db.Query`) and you don't want to initialize the `WebGrid` helper yet either.</span></span> <span data-ttu-id="48820-286">実行する必要がある SQL ステートメントを特定した後に、これらの処理を行います。</span><span class="sxs-lookup"><span data-stu-id="48820-286">You'll do those things after you've figured out which SQL statement has to run.</span></span>

<span data-ttu-id="48820-287">この書き換えられたブロックの後に、検索を処理するための新しいロジックを追加できます。</span><span class="sxs-lookup"><span data-stu-id="48820-287">After this rewritten block, you can add the new logic for handling the search.</span></span> <span data-ttu-id="48820-288">完成したコードは次のようになります。</span><span class="sxs-lookup"><span data-stu-id="48820-288">The completed code will look like the following.</span></span> <span data-ttu-id="48820-289">次の例に一致するように、ページ内のコードを更新します。</span><span class="sxs-lookup"><span data-stu-id="48820-289">Update the code in your page so it matches this example:</span></span>

[!code-cshtml[Main](form-basics/samples/sample8.cshtml)]

<span data-ttu-id="48820-290">これで、このページは次のように機能します。</span><span class="sxs-lookup"><span data-stu-id="48820-290">The page now works like this.</span></span> <span data-ttu-id="48820-291">このコードは、ページが実行されるたびにデータベースを開き、`selectCommand` 変数は、`Movies` テーブルからすべてのレコードを取得する SQL ステートメントに設定されます。</span><span class="sxs-lookup"><span data-stu-id="48820-291">Every time the page runs, the code opens the database and the `selectCommand` variable is set to the SQL statement that gets all the records from the `Movies` table.</span></span> <span data-ttu-id="48820-292">このコードでは、`searchTerm` 変数も初期化します。</span><span class="sxs-lookup"><span data-stu-id="48820-292">The code also initializes the `searchTerm` variable.</span></span>

<span data-ttu-id="48820-293">ただし、現在の要求に `searchGenre` 要素の値が含まれている場合、コードは `selectCommand` を別のクエリ (つまり、ジャンルを検索する `Where` 句を含む) に設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-293">However, if the current request includes a value for the `searchGenre` element, the code sets `selectCommand` to a different query — namely, to one that includes the `Where` clause to search for a genre.</span></span> <span data-ttu-id="48820-294">また、`searchTerm` を、検索ボックスに渡されたものにも設定します (これは何もできません)。</span><span class="sxs-lookup"><span data-stu-id="48820-294">It also sets `searchTerm` to whatever was passed for the search box (which might be nothing).</span></span>

<span data-ttu-id="48820-295">`selectCommand`にある SQL ステートメントに関係なく、コードは `db.Query` を呼び出してクエリを実行し、`searchTerm`にあるものを渡します。</span><span class="sxs-lookup"><span data-stu-id="48820-295">Regardless of which SQL statement is in `selectCommand`, the code then calls `db.Query` to run the query, passing it whatever is in `searchTerm`.</span></span> <span data-ttu-id="48820-296">`searchTerm`に何も含まれていない場合は、そのような場合でも、`selectCommand` に値を渡すパラメーターはありません。</span><span class="sxs-lookup"><span data-stu-id="48820-296">If there's nothing in `searchTerm`, it doesn't matter, because in that case there's no parameter to pass the value to `selectCommand` anyway.</span></span>

<span data-ttu-id="48820-297">最後に、このコードでは、前と同様に、クエリ結果を使用して `WebGrid` ヘルパーを初期化します。</span><span class="sxs-lookup"><span data-stu-id="48820-297">Finally, the code initializes the `WebGrid` helper by using the query results, just like before.</span></span>

<span data-ttu-id="48820-298">SQL ステートメントと検索語句を変数に配置することで、コードに柔軟性を追加したことがわかります。</span><span class="sxs-lookup"><span data-stu-id="48820-298">You can see that by putting the SQL statement and the search term into variables, you've added flexibility to the code.</span></span> <span data-ttu-id="48820-299">このチュートリアルで後ほど説明するように、この基本的なフレームワークを使用して、さまざまな種類の検索のロジックを追加することができます。</span><span class="sxs-lookup"><span data-stu-id="48820-299">As you'll see later in this tutorial, you can use this basic framework and keep adding logic for different types of searches.</span></span>

## <a name="testing-the-search-by-genre-feature"></a><span data-ttu-id="48820-300">ジャンル別検索機能のテスト</span><span class="sxs-lookup"><span data-stu-id="48820-300">Testing the Search-by-Genre Feature</span></span>

<span data-ttu-id="48820-301">WebMatrix で、[*ムービー* ] ページを実行します。</span><span class="sxs-lookup"><span data-stu-id="48820-301">In WebMatrix, run the *Movies.cshtml* page.</span></span> <span data-ttu-id="48820-302">ジャンルのテキストボックスを含むページが表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-302">You see the page with the text box for genre.</span></span>

<span data-ttu-id="48820-303">テストレコードのいずれかに入力したジャンルを入力し、 **[検索]** をクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-303">Enter a genre that you've entered for one of your test records, then click **Search**.</span></span> <span data-ttu-id="48820-304">今度は、そのジャンルに一致する映画だけが一覧表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-304">This time you see a listing of just the movies that match that genre:</span></span>

![ジャンル ' Comedies ' を検索した後のムービーページリスト](form-basics/_static/image4.png)

<span data-ttu-id="48820-306">別のジャンルを入力して、もう一度検索してください。</span><span class="sxs-lookup"><span data-stu-id="48820-306">Enter a different genre and search again.</span></span> <span data-ttu-id="48820-307">検索で大文字と小文字が区別されないことを確認できるように、すべて小文字または大文字を使用してジャンルを入力してみてください。</span><span class="sxs-lookup"><span data-stu-id="48820-307">Try entering the genre by using all lowercase or all uppercase letters so that you can see that the search is not case sensitive.</span></span>

## <a name="remembering-what-the-user-entered"></a><span data-ttu-id="48820-308">ユーザーが入力した内容を記憶する</span><span class="sxs-lookup"><span data-stu-id="48820-308">"Remembering" What the User Entered</span></span>

<span data-ttu-id="48820-309">ジャンルを入力し、 **[ジャンルの検索]** をクリックすると、そのジャンルの一覧が表示されていることがわかります。</span><span class="sxs-lookup"><span data-stu-id="48820-309">You might have noticed that after you entered a genre and clicked **Search Genre**, you saw a listing for that genre.</span></span> <span data-ttu-id="48820-310">ただし、検索テキストボックスは空の &mdash;、入力した内容を覚えていませんでした。</span><span class="sxs-lookup"><span data-stu-id="48820-310">However, the search text box was empty &mdash; in other words, it didn't remember what you'd entered.</span></span>

<span data-ttu-id="48820-311">この動作が発生する理由を理解しておくことが重要です。</span><span class="sxs-lookup"><span data-stu-id="48820-311">It's important to understand why this behavior occurs.</span></span> <span data-ttu-id="48820-312">ページを送信すると、ブラウザーは web サーバーに要求を送信します。</span><span class="sxs-lookup"><span data-stu-id="48820-312">When you submit a page, the browser sends a request to the web server.</span></span> <span data-ttu-id="48820-313">ASP.NET が要求を取得すると、ページの新しいインスタンスが作成され、その中のコードが実行されて、ブラウザーに再びページが表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-313">When ASP.NET gets the request, it creates a brand-new instance of the page, runs the code in it, and then renders the page to the browser again.</span></span> <span data-ttu-id="48820-314">しかし、実際には、以前のバージョンのを操作していたことはわかりません。</span><span class="sxs-lookup"><span data-stu-id="48820-314">In effect, though, the page doesn't know that you were just working with a previous version of itself.</span></span> <span data-ttu-id="48820-315">認識できるのは、何らかのフォームデータを含む要求を受け取ったことだけです。</span><span class="sxs-lookup"><span data-stu-id="48820-315">All it knows is that it got a request that had some form data in it.</span></span>

<span data-ttu-id="48820-316">ページを初めて要求する場合でも、ページを送信する場合でも、新しいページが表示される &mdash; には、ページを要求するたびに &mdash; ます。</span><span class="sxs-lookup"><span data-stu-id="48820-316">Every time you request a page &mdash; whether for the first time or by submitting it &mdash; you're getting a new page.</span></span> <span data-ttu-id="48820-317">Web サーバーには、前回の要求のメモリがありません。</span><span class="sxs-lookup"><span data-stu-id="48820-317">The web server has no memory of your last request.</span></span> <span data-ttu-id="48820-318">どちらも ASP.NET ではなく、どちらもブラウザーを行いません。</span><span class="sxs-lookup"><span data-stu-id="48820-318">Neither does ASP.NET, and neither does the browser.</span></span> <span data-ttu-id="48820-319">このページの個別のインスタンス間の接続は、これらのインスタンス間で送信されるデータだけです。</span><span class="sxs-lookup"><span data-stu-id="48820-319">The only connection between these separate instances of the page is any data that you transmit between them.</span></span> <span data-ttu-id="48820-320">たとえば、ページを送信する場合、新しいページインスタンスでは、前のインスタンスによって送信されたフォームデータを取得できます。</span><span class="sxs-lookup"><span data-stu-id="48820-320">If you submit a page, for example, the new page instance can get the form data that was sent by the earlier instance.</span></span> <span data-ttu-id="48820-321">(ページ間でデータを渡すもう1つの方法は、cookie を使用することです)。</span><span class="sxs-lookup"><span data-stu-id="48820-321">(Another way to pass data between pages is to use cookies.)</span></span>

<span data-ttu-id="48820-322">この状況を説明するための正式な方法は、web ページが*ステートレス*であるということです。</span><span class="sxs-lookup"><span data-stu-id="48820-322">A formal way to describe this situation is to say that web pages are *stateless*.</span></span> <span data-ttu-id="48820-323">Web サーバー、ページ自体、およびページ内の要素は、ページの以前の状態に関する情報を保持しません。</span><span class="sxs-lookup"><span data-stu-id="48820-323">Web servers and the pages themselves and the elements in the page do not maintain any information about the previous state of a page.</span></span> <span data-ttu-id="48820-324">Web はこのように設計されています。これは、個々の要求の状態を維持することで web サーバーのリソースを迅速に消費するためです。多くの場合、1秒あたり数百単位の要求を処理します。</span><span class="sxs-lookup"><span data-stu-id="48820-324">The web was designed this way because maintaining state for individual requests would quickly exhaust the resources of web servers, which often handle thousands, maybe even hundreds of thousands, of requests per second.</span></span>

<span data-ttu-id="48820-325">このため、テキストボックスが空でした。</span><span class="sxs-lookup"><span data-stu-id="48820-325">So that's why the text box was empty.</span></span> <span data-ttu-id="48820-326">ページを送信した後、ASP.NET はページの新しいインスタンスを作成し、コードとマークアップを実行します。</span><span class="sxs-lookup"><span data-stu-id="48820-326">After you submitted the page, ASP.NET created a new instance of the page and ran through the code and markup.</span></span> <span data-ttu-id="48820-327">ASP.NET がテキストボックスに値を設定するように指示したコードには何もありませんでした。</span><span class="sxs-lookup"><span data-stu-id="48820-327">There was nothing in that code that told ASP.NET to put a value into the text box.</span></span> <span data-ttu-id="48820-328">ASP.NET は何も実行しなかったので、テキストボックスは値なしでレンダリングされました。</span><span class="sxs-lookup"><span data-stu-id="48820-328">So ASP.NET didn't do anything, and the text box was rendered without a value in it.</span></span>

<span data-ttu-id="48820-329">実際には、この問題を回避するための簡単な方法があります。</span><span class="sxs-lookup"><span data-stu-id="48820-329">There's actually an easy way to get around this issue.</span></span> <span data-ttu-id="48820-330">テキストボックスに入力したジャンル*は*、`Request.QueryString["searchGenre"]`にある &mdash; コードで使用できます。</span><span class="sxs-lookup"><span data-stu-id="48820-330">The genre that you entered into the text box *is* available to you in code &mdash; it's in `Request.QueryString["searchGenre"]`.</span></span>

<span data-ttu-id="48820-331">次の例のように、テキストボックスのマークアップを更新して、`value` 属性が `searchTerm`から値を取得するようにします。</span><span class="sxs-lookup"><span data-stu-id="48820-331">Update the markup for the text box so that the `value` attribute gets its value from `searchTerm`, like this example:</span></span>

[!code-html[Main](form-basics/samples/sample9.html?highlight=1)]

<span data-ttu-id="48820-332">このページでは、`value` 属性を `searchTerm` 変数に設定することもできます。この変数には、入力したジャンルが含まれているためです。</span><span class="sxs-lookup"><span data-stu-id="48820-332">In this page, you could have also set the `value` attribute to the `searchTerm` variable, since that variable also contains the genre you entered.</span></span> <span data-ttu-id="48820-333">ただし、このタスクを実行するには、次に示すように、`Request` オブジェクトを使用して、`value` 属性を設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-333">But using the `Request` object to set the `value` attribute as shown here is the standard way to accomplish this task.</span></span> <span data-ttu-id="48820-334">(この &mdash; を実行する場合は、フィールドに値を*指定せず*にページを表示することもできます。</span><span class="sxs-lookup"><span data-stu-id="48820-334">(Assuming you even want to do this &mdash; in some situations, you might want to render the page *without* values in the fields.</span></span> <span data-ttu-id="48820-335">これは、アプリで何が起こっているかによって異なります)。</span><span class="sxs-lookup"><span data-stu-id="48820-335">It all depends on what's going on with your app.)</span></span>

> [!NOTE]
> <span data-ttu-id="48820-336">パスワードに使用されるテキストボックスの値を「記憶」することはできません。</span><span class="sxs-lookup"><span data-stu-id="48820-336">You can't "remember" the value of a text box that's used for passwords.</span></span> <span data-ttu-id="48820-337">コードを使用してパスワードフィールドを入力することを許可するセキュリティホールです。</span><span class="sxs-lookup"><span data-stu-id="48820-337">It would be a security hole to allow people to fill in a password field by using code.</span></span>

<span data-ttu-id="48820-338">もう一度ページを実行し、ジャンルを入力して、 **[ジャンルの検索]** をクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-338">Run the page again, enter a genre, and click **Search Genre**.</span></span> <span data-ttu-id="48820-339">今回は、検索結果が表示されるだけでなく、最後に入力した内容をテキストボックスに記憶します。</span><span class="sxs-lookup"><span data-stu-id="48820-339">This time not only do you see the results of the search, but the text box remembers what you entered last time:</span></span>

![テキストボックスに前のエントリが "記憶されました" と表示されていることを示すページ](form-basics/_static/image5.png)

## <a name="searching-for-any-word-in-the-title"></a><span data-ttu-id="48820-341">タイトル内の任意の単語を検索する</span><span class="sxs-lookup"><span data-stu-id="48820-341">Searching for Any Word in the Title</span></span>

<span data-ttu-id="48820-342">ジャンルを検索できるようになりましたが、タイトルを検索することもできます。</span><span class="sxs-lookup"><span data-stu-id="48820-342">You can now search for any genre, but you might also want to search for a title.</span></span> <span data-ttu-id="48820-343">検索時にタイトルを正確に取得するのは難しいので、代わりに、タイトル内の任意の場所に表示される単語を検索できます。</span><span class="sxs-lookup"><span data-stu-id="48820-343">It's hard to get a title exactly right when you search, so instead you can search for a word that appears anywhere inside a title.</span></span> <span data-ttu-id="48820-344">SQL でこれを行うには、次のように `LIKE` 演算子と構文を使用します。</span><span class="sxs-lookup"><span data-stu-id="48820-344">To do that in SQL, you use the `LIKE` operator and syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Title LIKE '%adventure%'`

<span data-ttu-id="48820-345">このコマンドは、タイトルに "adventure" が含まれているすべての映画を取得します。</span><span class="sxs-lookup"><span data-stu-id="48820-345">This command gets all the movies whose titles contain "adventure".</span></span> <span data-ttu-id="48820-346">`LIKE` 演算子を使用する場合は、検索語句の一部としてワイルドカード文字 `%` を含めます。</span><span class="sxs-lookup"><span data-stu-id="48820-346">When you use the `LIKE` operator, you include the wildcard character `%` as part of the search term.</span></span> <span data-ttu-id="48820-347">検索 `LIKE 'adventure%'` は、"adventure ' で始まる" という意味です。</span><span class="sxs-lookup"><span data-stu-id="48820-347">The search `LIKE 'adventure%'` means "starting with 'adventure'".</span></span> <span data-ttu-id="48820-348">(技術的には、"文字列" adventure "の後に何かが続くことを意味します)。同様に、検索用語 `LIKE '%adventure'` は "adventure" という文字列が続くことを意味します。これは、"adventure ' で終わる" というもう1つの方法です。</span><span class="sxs-lookup"><span data-stu-id="48820-348">(Technically, it means "The string 'adventure' followed by anything.") Similarly, the search term `LIKE '%adventure'` means "anything followed by the string 'adventure'", which is another way to say "ending with 'adventure'".</span></span>

<span data-ttu-id="48820-349">したがって、検索用語 `LIKE '%adventure%'`、タイトルのどこかに "adventure" があることを意味します。</span><span class="sxs-lookup"><span data-stu-id="48820-349">The search term `LIKE '%adventure%'` therefore means "with 'adventure' anywhere in the title."</span></span> <span data-ttu-id="48820-350">(技術的には、"タイトルに含まれるもの、" adventure "、その後に何かが続きます。")</span><span class="sxs-lookup"><span data-stu-id="48820-350">(Technically, "anything in the title, followed by 'adventure', followed by anything.")</span></span>

<span data-ttu-id="48820-351">`<form>` 要素内で、ジャンル検索の終了 `</div>` タグのすぐ下に、次のマークアップを追加します (終了 `</form>` 要素の直前)。</span><span class="sxs-lookup"><span data-stu-id="48820-351">Inside the `<form>` element, add the following markup right under the closing `</div>` tag for the genre search (just before the closing `</form>` element):</span></span>

[!code-html[Main](form-basics/samples/sample10.html)]

<span data-ttu-id="48820-352">この検索を処理するコードは、ジャンル検索のコードに似ていますが、`LIKE` 検索をアセンブルする必要がある点が異なります。</span><span class="sxs-lookup"><span data-stu-id="48820-352">The code to handle this search is similar to the code for the genre search, except that you have to assemble the `LIKE` search.</span></span> <span data-ttu-id="48820-353">ページの上部にあるコードブロック内で、次の `if` ブロックを、ジャンル検索の `if` ブロックの直後に追加します。</span><span class="sxs-lookup"><span data-stu-id="48820-353">Inside the code block at the top of the page, add this `if` block just after the `if` block for the genre search:</span></span>

[!code-csharp[Main](form-basics/samples/sample11.cs)]

<span data-ttu-id="48820-354">このコードでは、前に見たものと同じロジックを使用します。ただし、検索では `LIKE` 演算子が使用され、コードでは検索語句の前後に "`%`" が挿入される点が異なります。</span><span class="sxs-lookup"><span data-stu-id="48820-354">This code uses the same logic you saw earlier, except that the search uses a `LIKE` operator and the code puts "`%`" before and after the search term.</span></span>

<span data-ttu-id="48820-355">ページに別の検索を簡単に追加できることに注目してください。</span><span class="sxs-lookup"><span data-stu-id="48820-355">Notice how it was easy to add another search to the page.</span></span> <span data-ttu-id="48820-356">必要な作業は次のとおりです。</span><span class="sxs-lookup"><span data-stu-id="48820-356">All you had to do was:</span></span>

- <span data-ttu-id="48820-357">関連する検索ボックスに値があるかどうかをテストする `if` ブロックを作成します。</span><span class="sxs-lookup"><span data-stu-id="48820-357">Create an `if` block that tested to see whether the relevant search box had a value.</span></span>
- <span data-ttu-id="48820-358">`selectCommand` 変数を新しい SQL ステートメントに設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-358">Set the `selectCommand` variable to a new SQL statement.</span></span>
- <span data-ttu-id="48820-359">`searchTerm` 変数に、クエリに渡す値を設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-359">Set the `searchTerm` variable to the value to pass to the query.</span></span>

<span data-ttu-id="48820-360">次に、タイトル検索の新しいロジックを含む完全なコードブロックを示します。</span><span class="sxs-lookup"><span data-stu-id="48820-360">Here's the complete code block, which contains the new logic for a title search:</span></span>

[!code-cshtml[Main](form-basics/samples/sample12.cshtml)]

<span data-ttu-id="48820-361">このコードの動作の概要を次に示します。</span><span class="sxs-lookup"><span data-stu-id="48820-361">Here's a summary of what this code does:</span></span>

- <span data-ttu-id="48820-362">変数 `searchTerm` と `selectCommand` は、上部で初期化されます。</span><span class="sxs-lookup"><span data-stu-id="48820-362">The variables `searchTerm` and `selectCommand` are initialized at the top.</span></span> <span data-ttu-id="48820-363">これらの変数は、適切な検索用語 (存在する場合) と、ユーザーがページで実行した内容に基づいて適切な SQL コマンドに設定されます。</span><span class="sxs-lookup"><span data-stu-id="48820-363">You're going to set these variables to the appropriate search term (if any) and appropriate SQL command based on what the user does in the page.</span></span> <span data-ttu-id="48820-364">既定の検索は、データベースからすべてのムービーを取得するための単純なケースです。</span><span class="sxs-lookup"><span data-stu-id="48820-364">The default search is the simple case of getting all the movies from the database.</span></span>
- <span data-ttu-id="48820-365">`searchGenre` と `searchTitle`のテストでは、コードは検索する値に `searchTerm` を設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-365">In the tests for `searchGenre` and `searchTitle`, the code sets `searchTerm` to the value you want to search for.</span></span> <span data-ttu-id="48820-366">また、これらのコードブロックは、その検索用の適切な SQL コマンドに `selectCommand` を設定します。</span><span class="sxs-lookup"><span data-stu-id="48820-366">Those code blocks also set `selectCommand` to an appropriate SQL command for that search.</span></span>
- <span data-ttu-id="48820-367">`db.Query` メソッドは1回だけ呼び出され、任意の SQL コマンドが `selectedCommand` 内にあり、どの値も `searchTerm`に含まれています。</span><span class="sxs-lookup"><span data-stu-id="48820-367">The `db.Query` method is invoked only once, using whatever SQL command is in `selectedCommand` and whatever value is in `searchTerm`.</span></span> <span data-ttu-id="48820-368">検索語句がない場合 (ジャンルとタイトルの単語がない場合)、`searchTerm` の値は空の文字列になります。</span><span class="sxs-lookup"><span data-stu-id="48820-368">If there is no search term (no genre and no title word), the value of `searchTerm` is an empty string.</span></span> <span data-ttu-id="48820-369">ただし、そうではありません。この場合、クエリにはパラメーターは必要ありません。</span><span class="sxs-lookup"><span data-stu-id="48820-369">However, that doesn't matter, because in that case the query doesn't require a parameter.</span></span>

## <a name="testing-the-title-search-feature"></a><span data-ttu-id="48820-370">タイトル検索機能のテスト</span><span class="sxs-lookup"><span data-stu-id="48820-370">Testing the Title Search Feature</span></span>

<span data-ttu-id="48820-371">これで、完成した検索ページをテストできます。</span><span class="sxs-lookup"><span data-stu-id="48820-371">Now you can test your completed search page.</span></span> <span data-ttu-id="48820-372">*ムービーの*を実行します。</span><span class="sxs-lookup"><span data-stu-id="48820-372">Run *Movies.cshtml*.</span></span>

<span data-ttu-id="48820-373">ジャンルを入力し、 **[ジャンルの検索]** をクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-373">Enter a genre and click **Search Genre**.</span></span> <span data-ttu-id="48820-374">グリッドには、そのジャンルの映画 (前など) が表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-374">The grid displays movies of that genre, like before.</span></span>

<span data-ttu-id="48820-375">タイトルの単語を入力し、 **[検索タイトル]** をクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-375">Enter a title word and click **Search Title**.</span></span> <span data-ttu-id="48820-376">グリッドには、タイトルにその単語が含まれる映画が表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-376">The grid displays movies that have that word in the title.</span></span>

![タイトルで ' the ' を検索した後のムービーページリスト](form-basics/_static/image6.png)

<span data-ttu-id="48820-378">両方のテキストボックスを空白のままにして、いずれかのボタンをクリックします。</span><span class="sxs-lookup"><span data-stu-id="48820-378">Leave both text boxes blank and click either button.</span></span> <span data-ttu-id="48820-379">グリッドには、すべてのムービーが表示されます。</span><span class="sxs-lookup"><span data-stu-id="48820-379">The grid displays all the movies.</span></span>

## <a name="combining-the-queries"></a><span data-ttu-id="48820-380">クエリの結合</span><span class="sxs-lookup"><span data-stu-id="48820-380">Combining the Queries</span></span>

<span data-ttu-id="48820-381">実行できる検索条件は排他的です。</span><span class="sxs-lookup"><span data-stu-id="48820-381">You might notice that the searches you can perform are exclusive.</span></span> <span data-ttu-id="48820-382">両方の検索ボックスに値がある場合でも、タイトルとジャンルを同時に検索することはできません。</span><span class="sxs-lookup"><span data-stu-id="48820-382">You can't search the title and the genre at the same time, even if both search boxes have values in them.</span></span> <span data-ttu-id="48820-383">たとえば、タイトルに "Adventure" が含まれているすべてのアクションムービーを検索することはできません。</span><span class="sxs-lookup"><span data-stu-id="48820-383">For example, you can't search for all action movies whose title contains "Adventure".</span></span> <span data-ttu-id="48820-384">(ページが現在コーディングされているので、ジャンルとタイトルの両方に値を入力すると、タイトルの検索が優先されます)。条件を組み合わせた検索を作成するには、次のような構文を含む SQL クエリを作成する必要があります。</span><span class="sxs-lookup"><span data-stu-id="48820-384">(As the page is coded now, if you enter values for both genre and title, the title search gets precedence.) To create a search that combines the conditions, you would have to create a SQL query that has syntax like the following:</span></span>

`SELECT * FROM Movies WHERE Genre = @0 AND Title LIKE @1`

<span data-ttu-id="48820-385">また、次のようなステートメントを使用してクエリを実行する必要があります (おおよその話)。</span><span class="sxs-lookup"><span data-stu-id="48820-385">And you'd have to run the query by using a statement like the following (roughly speaking):</span></span>

`var selectedData = db.Query(selectCommand, searchGenre, searchTitle);`

<span data-ttu-id="48820-386">検索条件の多くの順列を可能にするロジックを作成することは、ご覧のように、少し複雑になることがあります。</span><span class="sxs-lookup"><span data-stu-id="48820-386">Creating logic to allow many permutations of search criteria can get a bit involved, as you can see.</span></span> <span data-ttu-id="48820-387">そのため、ここでは停止します。</span><span class="sxs-lookup"><span data-stu-id="48820-387">Therefore, we'll stop here.</span></span>

## <a name="coming-up-next"></a><span data-ttu-id="48820-388">次へ</span><span class="sxs-lookup"><span data-stu-id="48820-388">Coming Up Next</span></span>

<span data-ttu-id="48820-389">次のチュートリアルでは、フォームを使用するページを作成して、ユーザーがムービーをデータベースに追加できるようにします。</span><span class="sxs-lookup"><span data-stu-id="48820-389">In the next tutorial, you'll create a page that uses a form to let users add movies to the database.</span></span>

## <a name="complete-listing-for-movie-page-updated-with-search"></a><span data-ttu-id="48820-390">[ムービーの完全な一覧表示] ページ (検索で更新)</span><span class="sxs-lookup"><span data-stu-id="48820-390">Complete Listing for Movie Page (Updated with Search)</span></span>

[!code-cshtml[Main](form-basics/samples/sample13.cshtml)]

## <a name="additional-resources"></a><span data-ttu-id="48820-391">その他のリソース</span><span class="sxs-lookup"><span data-stu-id="48820-391">Additional Resources</span></span>

- [<span data-ttu-id="48820-392">Razor 構文を使用した ASP.NET Web プログラミングの概要</span><span class="sxs-lookup"><span data-stu-id="48820-392">Introduction to ASP.NET Web Programming Using the Razor Syntax</span></span>](https://go.microsoft.com/fwlink/?LinkID=202890)
- <span data-ttu-id="48820-393">W3Schools サイトの[SQL WHERE 句](http://www.w3schools.com/sql/sql_where.asp)</span><span class="sxs-lookup"><span data-stu-id="48820-393">[SQL WHERE Clause](http://www.w3schools.com/sql/sql_where.asp) on the W3Schools site</span></span>
- <span data-ttu-id="48820-394">W3C サイトの[メソッド定義](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html)に関する記事</span><span class="sxs-lookup"><span data-stu-id="48820-394">[Method Definitions](http://www.w3.org/Protocols/rfc2616/rfc2616-sec9.html) article on the W3C site</span></span>

> [!div class="step-by-step"]
> <span data-ttu-id="48820-395">[前へ](displaying-data.md)
> [次へ](entering-data.md)</span><span class="sxs-lookup"><span data-stu-id="48820-395">[Previous](displaying-data.md)
[Next](entering-data.md)</span></span>
